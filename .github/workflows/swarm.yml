name: renker-swarm

# Runs on GitHub's servers (not your PC), on a schedule. GitHub's minimum cron
# interval is 5 minutes; scheduled runs can be delayed under load, and GitHub
# disables schedules after 60 days without repo activity — a manual run or any
# push re-arms them.
on:
  schedule:
    # Every 30 min. On a PRIVATE repo GitHub gives 2000 free Action-minutes/month;
    # ~48 runs/day x ~1 min stays within that (if it ever runs out, GitHub just
    # pauses — it never bills a free account). Want faster / unlimited? Make the
    # repo public (unlimited Action minutes) or lower this interval.
    - cron: "*/30 * * * *"
  workflow_dispatch: {}          # + a manual "Run workflow" button

permissions:
  contents: write                # allow committing outputs/state back to the repo

concurrency:
  group: renker-swarm
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt

      - name: Run one cycle
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
        run: python run_once.py

      - name: Commit results
        run: |
          git config user.name "renker-swarm"
          git config user.email "swarm@renker.local"
          git add outputs state docs
          git diff --staged --quiet || git commit -m "swarm: $(date -u +%FT%TZ)"
          git push
